buildscript {
	repositories {
		mavenLocal()
		mavenCentral()
		maven {
			name = "sonatype"
			url = "https://oss.sonatype.org/content/repositories/snapshots/"
		}
		maven {
			name = "forge"
			url = "https://files.minecraftforge.net/maven"
		}
		maven {
			name = 'sponge'
			url = 'https://repo.spongepowered.org/maven'
		}
	}
	dependencies {
		classpath 'net.minecraftforge.gradle:ForgeGradle:2.3-SNAPSHOT'
		classpath 'org.spongepowered:mixingradle:0.4-SNAPSHOT'
	}
}

apply plugin: 'net.minecraftforge.gradle.liteloader'
apply plugin: 'org.spongepowered.mixin'

group = 'rsmm_lite'
version = '1.6.2'
archivesBaseName = "redstone-multimeter-lite-mc${project.mcVersion}"

minecraft {
	version = project.mcVersion
	mappings = project.mcpMappings
	runDir = "run"
}

sourceSets {
	main {
		// Refmap declaration must match the refmap name specified in the json config
		ext.refMap = "mixins.rsmm_lite.refmap.json"
	}
}

litemod {
	json{
		name = "rsmm_lite"
		mcversion = project.mcVersion
		description = "A remake of NarcolepticFrog's Redstone Multimeter mod."
		mixinConfigs += "mixins.rsmm_lite.json"
	}
}

mixin {
	defaultObfuscationEnv notch
}

jar {
	from litemod.outputs
}
